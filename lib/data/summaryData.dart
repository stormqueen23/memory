class SummaryData {
  final int stars;
  final int moves;
  final int points;
  final bool isLastLevel;
  final bool failed;
  final bool allSummary;
  final String backgroundImage;

  SummaryData(this.stars, this.moves, this.points, this.isLastLevel, this.failed, this.allSummary, this.backgroundImage);
}